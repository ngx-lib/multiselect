import { DisplaySelectedValuePipe } from './display-selected-value.pipe';

describe('DisplaySelectedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaySelectedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
